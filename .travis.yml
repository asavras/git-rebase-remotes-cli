language: python

python:
  - "2.7"
  - "3.6"

install:
  - pip install -U pip
  - pip install -r requirements.txt

script:
  - flake8 --max-line-length 120 rebase-remotes.py
  - pytest -p no:cacheprovider